import React from 'react';
import { Typography, Box } from '@mui/material';

function SignOut() {
    return (
        <Box>
            <Typography variant="h4">Sign Out</Typography>
            <Typography>This is the Sign Out page.</Typography>
        </Box>
    );
}

export default SignOut;
