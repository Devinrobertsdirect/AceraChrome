import React from 'react';
import { Typography, Box } from '@mui/material';

function Saved() {
    return (
        <Box>
            <Typography variant="h4">Saved</Typography>
            <Typography>This is the Saved page.</Typography>
        </Box>
    );
}

export default Saved;
