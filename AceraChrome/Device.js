import React from 'react';
import { Typography, Box } from '@mui/material';

function Device() {
    return (
        <Box>
            <Typography variant="h4">Device</Typography>
            <Typography>This is the Device page.</Typography>
        </Box>
    );
}

export default Device;
